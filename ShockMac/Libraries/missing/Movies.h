
#warning this file was created because it was missing, size of types, or order of type might be wrong.

enum 
{
	fsRdPerm = 1
};

enum
{
	newMovieActive = 1
};

enum
{
	kFullVolume = 1
};

enum
{
	createMovieFileDeleteCurFile = 1
};

enum
{
	VideoMediaType = 1
};

enum
{
	StandardCompressionType = 1,
	StandardCompressionSubType
};
